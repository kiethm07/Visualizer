#pragma once
#include <vector>

enum class TrieAnimationType { HighlightOn, HighlightOff, FadeIn, FadeOut, Wait, Move, Spawn, FoundedOn, FoundedOff, Remove };
enum class TrieAnimationTarget { Node, Edge, Bucket };