#pragma once
#include <vector>

enum class LinkedListAnimationType{ HighlightOn, HighlightOff, FadeIn, FadeOut, FoundedOn, FoundedOff, UpdateValue, Wait, Move, Spawn, Remove };
enum class LinkedListAnimationTarget { Node, Edge };